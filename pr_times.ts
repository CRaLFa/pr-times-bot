import { DOMParser, Element } from '@b-fuze/deno-dom';

const PR_TIMES_URL = 'https://prtimes.jp/main/html/newarrival';

const searchPressRelease = async (lastTime: number, searchCond: RegExp) => {
};

export { searchPressRelease };
